# IBM Quantum Requirements
# Install with: pip install -r requirements_ibm.txt

# Core IBM Quantum packages
qiskit>=1.0.0
qiskit-ibm-runtime>=0.15.0
qiskit-aer>=0.13.0

# PennyLane with Qiskit plugin
pennylane>=0.33.0
pennylane-qiskit>=0.33.0

# Machine Learning (should already be installed)
torch>=2.0.0
numpy>=1.23.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0

# Progress bars
tqdm>=4.65.0

# Jupyter (if not already installed)
jupyter>=1.0.0
notebook>=6.5.0
